use std::collections::HashMap;

use crate::parser::{triple::Triple, Stuff};

/*
pub fn update_triples(stuff: Stuff, dict: &mut HashMap<String, u32>) {
    for triple in stuff.triples {
        let mut new_key = generate_new_id(dict);
        for (sn1, sn11) in stuff.supernodes.into_iter() {

        }
    }
}*/

fn write_to_file() {

}

